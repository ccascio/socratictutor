# Required — get yours at https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# Optional — set a base path when deploying under a subpath (e.g. /app)
# NEXT_PUBLIC_BASE_PATH=

# Set automatically by the desktop app (Tauri). Do not set manually.
# SOCRATIC_DATA_DIR=
